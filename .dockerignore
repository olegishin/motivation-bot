# Python virtual environment
venv/
env/
ENV/

# IDE and editor configuration
.vscode/
.idea/
*.swp
*.bak
*.tmp

# Python cache and temporary files
__pycache__/
*.pyc
*.pyo
*.log

# Build artifacts
*.egg-info/
dist/
build/

# Local environment file (КРИТИЧЕСКИ ВАЖНО!)
.env
token_id.env
*.env

# Служебные файлы fly
flyctl.exe

# Git (не нужно в продакшене)
.git/
.gitignore
.gitattributes

# Тесты (если не нужны)
tests/
pytest.ini
*.pytest_cache/

# Документация (опционально)
README.md
docs/

# Локальная база данных (если есть)
fotinia.db
*.db-shm
*.db-wal

# Резервные копии
backup_*.json
*.backup

# НЕ ВКЛЮЧАЕМ templates/ - они НУЖНЫ в продакшене!
# НЕ ВКЛЮЧАЕМ static/ - они НУЖНЫ в продакшене!
# НЕ ВКЛЮЧАЕМ fly.toml - он НУЖЕН для Fly.io!
# НЕ ВКЛЮЧАЕМ data_initial/ - она НУЖНА для контента!